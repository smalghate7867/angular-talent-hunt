import { FilterRecruitPagePipe } from './filter-recruit-page.pipe';

describe('FilterRecruitPagePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRecruitPagePipe();
    expect(pipe).toBeTruthy();
  });
});
